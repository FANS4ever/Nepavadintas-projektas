@model List<Gear.Models.Game>

@{
    ViewBag.Title = "Store";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="">
    <h1 class="display-4">Featured</h1>

    <div id="featured" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img class="d-block w-100 img-rounded" src="@("../Files/" + Model[0].Dir + "/icon.jpg")" alt="First slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100 img-rounded" src="@("../Files/" + Model[0].Dir + "/icon.jpg")" alt="Second slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100 img-rounded" src="@("../Files/" + Model[0].Dir + "/icon.jpg")" alt="Third slide">
            </div>
        </div>
        <ul class="carousel-indicators">
            <li data-target="#featured" data-slide-to="0" class="active"></li>
            <li data-target="#featured" data-slide-to="1"></li>
            <li data-target="#featured" data-slide-to="2"></li>
        </ul>
    </div>
</div>
<div>
    tba
</div>
<div>
    tba
</div>